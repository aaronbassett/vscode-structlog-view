# VS Code Structlog Extension

This is a demo project and is not intended for production use

## Running the example

* npm install
* npm run watch or npm run compile
* F5 (in VS Code) to start debugging